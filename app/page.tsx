export default function Page() {
  // Placeholder — the Three.js GameCanvas mounts here in step 3.
  return (
    <main
      style={{
        position: 'fixed',
        inset: 0,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'var(--font-baloo), sans-serif',
        fontWeight: 800,
        fontSize: 24,
        color: '#1d2b36',
      }}
    >
      Brick Voyage — scaffolding ready. Game canvas lands here.
    </main>
  );
}
